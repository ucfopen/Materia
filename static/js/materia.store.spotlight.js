// Generated by CoffeeScript 1.6.3
(function() {
  Namespace('Materia.Store').Spotlight = (function() {
    var get, init, populateSpotlight, spotlightArray;
    spotlightArray = null;
    init = function(gateway) {};
    get = function(callback) {
      return Materia.Coms.Json.send('widget_spotlight_get', null, function(data) {
        spotlightArray = data;
        return populateSpotlight(function() {
          if (callback) {
            return callback();
          }
        });
      });
    };
    populateSpotlight = function(callback) {
      var checked, hide, i, spotlight, _i, _len;
      for (i = _i = 0, _len = spotlightArray.length; _i < _len; i = ++_i) {
        spotlight = spotlightArray[i];
        hide = '';
        checked = '';
        if (i !== 0) {
          hide = 'hidden';
        } else {
          checked = 'checked="checked"';
        }
        $('.main_container').append('<article class="store_main ' + hide + '" id="spolight_' + i + '" >' + spotlight + '</article>');
        $('.cycler').append('<input type="radio" name="spotlight" id="slide_' + i + '" ' + checked + ' class="radio_spotlight" />');
      }
      if (callback) {
        return callback();
      }
    };
    return {
      init: init,
      get: get
    };
  })();

}).call(this);
